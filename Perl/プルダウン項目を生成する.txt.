WEBインターフェイスをやっているときに必ず触るだろう「プルダウン」

それぞれに配置してもいいし、jsでせいせいしてもいい。
今回perlで処理が必要になったことを考えてサブルーチン化しておく

&pd(a,b,c); valueが１２３でいい場合
&pd(a=>AA,b=>BB,c=>CC);valueを指定する場合

あと、初期selectedの設定も追加
先頭に入れる。
戻り値は生成されたoption要素

